/**
 * Web video-studio plugin, node half.
 *
 * Deliberately empty. The studio is a browser overlay that formats a user
 * message; OpenMontage skills and the operating prompt stay on the host
 * adapter that a profile mounts separately.
 */

/** Host plugin body — production work belongs to `@deepseek-ai/dsh-openmontage`. */
export function apply(): void {}
