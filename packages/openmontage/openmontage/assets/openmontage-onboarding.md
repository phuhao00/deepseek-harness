# OpenMontage onboarding

The OpenMontage checkout is at `{{openmontage_root}}`.

Use this skill when the user's first message is vague or exploratory ("make me a video", "what can you do?").

Read `{{openmontage_root}}/skills/meta/onboarding.md` and follow it before any production work.

If the user arrived with a specific video request, load `openmontage` instead.
