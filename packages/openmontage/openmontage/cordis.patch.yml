# Opt-in OpenMontage adapter. `root` is required; the bundle reads
# OPENMONTAGE_ROOT so a missing env var fails Config validation at load.
- insert:
    - id: openmontage
      name: '@deepseek-ai/dsh-openmontage'
      config:
        root: !!js process.env.OPENMONTAGE_ROOT
