/**
 * @deepseek-ai/dsh-acp-app — the product ACP profile bundle. The package's
 * substance is `cordis.patch.yml`, declared by the `dsh.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API. The command-line provider lives at
 * `./startup`; the Agent Client Protocol server is `@deepseek-ai/dsh-acp`.
 * @module @deepseek-ai/dsh-acp-app
 */

export {}
